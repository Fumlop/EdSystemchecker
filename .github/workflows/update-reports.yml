name: Update PowerPlay Reports

on:
  # Manual trigger via GitHub UI
  workflow_dispatch:
  # Scheduled updates (every 4 hours during PowerPlay cycle)
  schedule:
    - cron: '0 */4 * * *'
  # Trigger on push to main branch
  push:
    branches: [ main ]

jobs:
  update-reports:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 lxml
        
    - name: Create html directory
      run: mkdir -p html
        
    - name: Download Inara HTML files
      run: |
        echo "🌐 Downloading PowerPlay data from Inara..."
        
        # Download Felicia Winters PowerPlay pages (Power ID 5)
        curl -s -o html/power-controlled-5.html "https://inara.cz/elite/powerplay-power-controlled/5/"
        curl -s -o html/power-exploited-5.html "https://inara.cz/elite/powerplay-power-exploited/5/"
        curl -s -o html/power-contested-5.html "https://inara.cz/elite/powerplay-power-contested/5/"
        
        echo "✅ Downloaded HTML files:"
        ls -la html/
        
    - name: Extract system data
      run: |
        echo "🔄 Extracting system data from HTML files..."
        python python/extract.py
        
    - name: Generate all reports
      run: |
        echo "📄 Generating PowerPlay reports..."
        python python/create_universal_md.py stronghold
        python python/create_universal_md.py fortified  
        python python/create_universal_md.py exploited
        python python/create_contested_md.py
        
    - name: Check for changes
      id: changes
      run: |
        git add *.md json/*.json html/*.html
        if git diff --staged --quiet; then
          echo "changes=false" >> $GITHUB_OUTPUT
        else
          echo "changes=true" >> $GITHUB_OUTPUT
        fi
        
    - name: Commit and push changes
      if: steps.changes.outputs.changes == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git commit -m "🤖 Auto-update PowerPlay reports - $(date '+%Y-%m-%d %H:%M:%S UTC')"
        git push
        
    - name: Summary
      run: |
        if [ "${{ steps.changes.outputs.changes }}" == "true" ]; then
          echo "✅ Reports updated and pushed to repository"
        else
          echo "ℹ️ No changes detected, repository up to date"
        fi
